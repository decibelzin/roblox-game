local RegisterCommand = require(game.ReplicatedStorage.Shared['register-command'])

RegisterCommand({
    name = "copyskin",
    description = "copia a skin do player",
    aliases = {"cs", "copy"}
}, function(playername, params, message)
    

end)